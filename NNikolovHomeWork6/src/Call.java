
public class Call {
	static double priceForAMin = 0.25; 
	String caller;  // GSM Number
	String receiver;  // GSM number
	int duration;  // counts only minutes and can be between 1 and 120. After 120min the call is automatically stoped
	
	
}
